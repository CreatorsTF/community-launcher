class Errors {
    /**
     * creates new Error containing a inner Error
     * @param {string} text 
     * @param {Error} innerError 
     */
    public static InnerError(text: string, innerError: Error) {
        text = text;
        innerError = innerError;
    }
}

export default Errors;